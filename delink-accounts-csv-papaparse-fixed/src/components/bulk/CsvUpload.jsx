import {Download,FileText,UploadCloud,X} from "lucide-react";import Card from "../common/Card";import Button from "../common/Button";export default function CsvUpload(p){return <Card className="overflow-hidden"><div className="px-7 pt-5"><h2 className="text-[15px] font-semibold">Upload CSV File</h2><p className="mt-1 text-xs text-slate-500">Upload a CSV file to de-link multiple accounts</p></div><div className="mx-7 my-4 flex min-h-[78px] flex-col gap-4 rounded-lg border border-slate-200 px-5 py-4 sm:flex-row sm:items-center sm:justify-between">{p.info?<div className="flex items-center gap-4"><FileText size={34} className="text-brand-500"/><div><p className="text-sm font-semibold">{p.info.name}</p><p className="mt-1 text-xs text-slate-500">{p.info.count} accounts · {p.info.size}</p></div></div>:<label className="flex cursor-pointer items-center gap-4"><UploadCloud size={35} className="text-brand-500"/><div><p className="text-sm font-semibold">Choose a CSV file</p><p className="mt-1 text-xs text-slate-500">.csv · Maximum 20 accounts</p></div><input type="file" accept=".csv,text/csv" onChange={p.onUpload} disabled={p.disabled} className="sr-only"/></label>}<div className="flex gap-2">{p.info&&<Button variant="icon" icon={X} onClick={p.onRemove} disabled={p.disabled} className="h-10 w-10 p-0"/>}<Button variant="secondary" icon={Download} onClick={p.onTemplate} disabled={p.disabled}>Download Template</Button></div></div>{p.error&&<div className="mx-7 mb-5 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{p.error}</div>}</Card>}