import Card from "../common/Card";import {CheckCircle2,CircleAlert,ListChecks} from "lucide-react";export default function ProcessingSummary({accounts}){if(!accounts.length||!accounts.every(a=>["De-linked","Failed"].includes(a.status)))return null;const ok=accounts.filter(a=>a.status==="De-linked").length,fail=accounts.filter(a=>a.status==="Failed").length;return <Card className="mt-5 p-5"><div className="flex items-center gap-2"><ListChecks size={18} className="text-brand-500"/><b className="text-sm">Processing Summary</b></div><div className="mt-4 grid gap-3 sm:grid-cols-3"><div className="rounded-lg bg-slate-50 p-3"><small>Total Accounts</small><p className="text-lg font-semibold">{accounts.length}</p></div><div className="rounded-lg bg-green-50 p-3"><span className="text-xs text-green-700"><CheckCircle2 size={14} className="inline"/> De-linked</span><p className="text-lg font-semibold text-green-700">{ok}</p></div><div className="rounded-lg bg-red-50 p-3"><span className="text-xs text-red-700"><CircleAlert size={14} className="inline"/> Failed</span><p className="text-lg font-semibold text-red-700">{fail}</p></div></div></Card>}