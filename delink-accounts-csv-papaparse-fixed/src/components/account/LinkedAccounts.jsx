import {Link2} from "lucide-react";import Card from "../common/Card";import Button from "../common/Button";export default function LinkedAccounts({account,onDelink,disabled}){if(!account)return null;const x=account.linkedAccountId?[{id:account.linkedAccountId,date:account.linkedOn}]:[];return <Card className="overflow-hidden"><div className="px-4 py-4"><h3 className="text-sm font-semibold">Linked Accounts ({x.length})</h3></div><div className="border-t border-slate-100">{x.length?x.map(a=><div key={a.id} className="flex justify-between border-b border-slate-100 px-4 py-4"><div><p className="text-sm font-medium">{a.id}</p><p className="mt-1 text-xs text-slate-500">Linked on {a.date}</p></div><span>-</span></div>):<p className="px-4 py-5 text-sm text-slate-500">No linked accounts.</p>}</div><div className="flex justify-end p-4"><Button icon={Link2} disabled={disabled||!x.length} onClick={onDelink}>De-Link Account</Button></div></Card>}