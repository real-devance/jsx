import {Search} from "lucide-react";import Card from "../common/Card";import Input from "../common/Input";import Button from "../common/Button";export default function SearchAccount(p){return <Card className="p-5"><h2 className="text-[15px] font-semibold">Search Account</h2><p className="mt-1 text-xs text-slate-500">Enter Account ID to view linked details</p><div className="mt-4 flex gap-3"><Input value={p.value} onChange={p.onChange} placeholder="Enter Account ID" error={p.error}/><Button icon={Search} onClick={p.onSearch} disabled={p.loading} className="h-[46px] shrink-0">{p.loading?"Searching...":"Search"}</Button></div>{p.found&&!p.error&&<div className="mt-4 rounded-lg border border-green-200 bg-green-50 px-4 py-3 text-sm font-medium text-green-700">✓ &nbsp;Account found</div>}</Card>}